#include "common.hpp"

float AnkiParams::questionX = float(TOP_SCREEN_WIDTH) / 2;
float AnkiParams::questionY = float(TOP_SCREEN_WIDTH) / 4;

float AnkiParams::answerX = float(TOP_SCREEN_WIDTH) / 2;
float AnkiParams::answerY = TOP_SCREEN_HEIGHT * 0.75;
